# Copy to `.env` in this directory

# Listen on all network interfaces (required so phones on LAN can connect)
# Do NOT use 127.0.0.1 / localhost only
HOST=0.0.0.0

# HTTP port — match PI_BASE_URL in the FastCub-Store .env (e.g. http://192.168.1.50:3000)
PORT=3000
